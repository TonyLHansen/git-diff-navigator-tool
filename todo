```markdown
# Project refactor TODO — history preparers

Goal: extract duplicated logic between file-mode and repo-mode history
preparers into small, testable helper methods. For each helper:

- implement helper in the indicated class
- update one `_prep` function to use it
- run `scripts/test_prep_backends.py ../test-repo` and `scripts/check_axioms.py`
- when green, update the corresponding other preparer to use it and test again

Workflow: go slowly, one helper at a time. Mark items as done as tests pass.

- [x] 1. Add `AppBase._run_git_lines(cmd: list[str], label: str|None) -> list[str]`
      # Project refactor TODO — history preparers

      Goal: extract duplicated logic between file-mode and repo-mode preparers into
      small, testable helpers. Work slowly: implement one helper, update one prep
      function to use it, run parity & axioms, then update the counterpart.

      Completed checklist (refactor items implemented and tested):

      - [x] 1. `AppBase._run_git_lines(cmd, label)` implemented
      - [x] 2. `AppBase._canonical_relpath(path, repo_root)` implemented
      - [x] 3. `AppBase._compute_pseudo_timestamps(repo_root, mods, single_path)` implemented
      - [x] 4. `AppBase._format_pseudo_summary(pseudo_entries)` implemented
      - [x] 5. `AppBase._parse_git_log_lines(lines)` implemented
      - [x] 6. `HistoryListBase._format_commit_row(dt, hash, msg)` implemented
      - [x] 7. `AppBase._append_file_row(display, full_path, is_dir=False, status=None)` implemented
      - [x] 8. `HistoryListBase._finalize_prep(curr_hash, prev_hash, path)` implemented
      - [x] 9. `AppBase._compare_backends` parity helper implemented
      - [x] 10. Ran full regression (`scripts/test_prep_backends.py`, `scripts/check_axioms.py`) — parity=0 diffs, axioms OK
      - [x] 11. Docstrings added to small helpers and logger wrappers

      File-list preparer refactor (implemented steps):

      - [x] 12. `FileListBase._list_directory(path)` implemented (safe os.listdir wrapper)
      - [x] 13. `FileListBase._build_status_map(path)` implemented (centralized status_map construction)
      - [x] 14. `FileListBase._populate_from_file_infos(file_infos)` implemented (centralized row creation)
      - [x] 15. `FileModeFileList.prepFileModeFileList` updated to use helpers; parity & axioms OK
      - [x] 16. `RepoModeFileList.prepRepoModeFileList` updated to use helpers; parity & axioms OK

      Next actions (suggested):

      - Run the full application to smoke-test UI behavior:

      ```bash
      python3 gitdiffnavtool.py
      ```

      - Continue extracting smaller helpers (e.g., `_render_parent_entry_if_needed`, `_render_hash_header`, `_schedule_highlight_and_visibility`) to further simplify prep functions.

      Testing reminders:

      After each helper change:

```bash
python3 scripts/test_prep_backends.py ../test-repo
python3 scripts/check_axioms.py
```

      Notes:

      - Keep helpers small and preserve `ListItem` metadata fields (`_raw_text`, `_filename`, `_is_dir`, `_repo_status`).
      - When a helper is exercised by parity tests, add a delegator to `scripts/test_prep_backends.py::Dummy` so both backends remain comparable.

      Status: file-list preparer refactor completed for implemented helpers; repository tests passing.


```
