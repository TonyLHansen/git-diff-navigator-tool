[gitdiffnavtool]
ignored-files = false
untracked-files = false
repo-first = false
initial-popup = false
color = red-green
# diff = word-diff
diff = side-by-side
# trim-debug = true
# debug = tmp/gdnt.log
# --- Added by --init-config: missing option hints ---
# Uncomment and adjust any options you want to set explicitly.
# Show author name and email in commit rows.
# add-authors = true
# Use a specific git branch.
# branch = main
# Directory where snapshot files are written.
# output-directory = /tmp/snapshots
# Display width for short commit hashes (must be >= 1).
# hash-length = 12
# Unified diff context lines (must be >= 0).
# unified-context = 3
# Maximum history entries to display (0 means unlimited).
# history-limit = 0
# Minimum terminal width for side-by-side view (must be >= 1).
# minimum-sidebyside-width = 60
